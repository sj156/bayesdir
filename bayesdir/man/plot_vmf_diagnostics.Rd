% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/all-helpers.R
\name{plot_vmf_diagnostics}
\alias{plot_vmf_diagnostics}
\title{Diagnostic plot for vMF MPS and MCMC inference}
\usage{
plot_vmf_diagnostics(mps_res, bayes_res, kappa_true, n, data_X)
}
\arguments{
\item{mps_res}{Output from \code{\link{run_predresamp_vmf}} with
\code{save_raw = TRUE}. Must contain \code{raw_chains} and
\code{samples$kappa_sam}.}

\item{bayes_res}{A matrix of posterior draws from
\code{\link{fit_mcmc_vmf}} or other method; the second column is assumed to hold
\eqn{\kappa} samples.}

\item{kappa_true}{True value of the concentration parameter (scalar).}

\item{n}{Sample size (scalar integer).}

\item{data_X}{data in cartesian coordinates}
}
\description{
Produces a three‑panel diagnostic figure for the von Mises–Fisher
concentration parameter \eqn{\kappa}:
\enumerate{
\item Left panel: raw‑score sample paths (one per MPS chain) over
resampling iterations.
\item Right panel: PC‑MPS density estimate overlaid with the MCMC
posterior density.
\item Bottom legend: initial estimator, true value, MPS density, and
MCMC benchmark.
}
}
\examples{
\dontrun{
# Assumes mps_res, bayes_res, kappa_true, and n already exist
plot_vmf_diagnostics(mps_res = mps_res,
                     bayes_res = bayes_res,
                     kappa_true = kappa_true,
                     n = n)
}

}
